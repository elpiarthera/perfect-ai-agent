# Diary Days 145 + 146 — publish EN + FR

- Day 145 "The Night I Sent the House Home" -> FR "La nuit où j'ai renvoyé la maison chez elle"
- Day 146 "The Honest Zero" -> FR "Le zéro honnête"

## RULE #7 — clean, sixth and seventh running

Independent sweep + full read of both: zero orchestrator names, zero client names,
positive control included. The upstream guard holds across 139-146.

## Defects caught (two separate review passes)

Day 145 (83/100 -> fixed):
- `un objectif, un précis` — "un précis" is not a standalone noun -> `un objectif, un seul, précis`.
- `portes dont j'avais besoin d'ouvrir` — wrong relative pronoun -> `que j'avais besoin d'ouvrir`.
- narrator gender: `j'étais invité`, `j'étais resté` -> recast to unmarked forms.
- `sans amortisseur` (mechanical) -> `sans ménagements`; `grandi monstrueux` -> `devenu monstrueux`.

Day 146 (81/100 -> fixed):
- `presque-réussis` for "near-misses" — **inverted polarity** (nearly succeeded vs nearly failed) -> `presque-manqués`.
- `émoussille` — **a verb that does not exist** -> `émousse`.
- `vérifies ce qui ne va pas` — dropped the concrete "plugged it in" image -> `vérifies si tu l'as branché`.
- `ce qu'un nom correct ressemble` — broken relative -> `ce à quoi ressemble un nom correct`.
- `Laisse-moi d'abord dire` — imperative calque foreign to the narrator's voice -> `Je vais d'abord raconter`.
- `l'arme regardait vers` — softened the menace of "pointing at" -> `l'arme pointait soudain vers`.
- `écrite par celle qui` — gendered the client where EN says "the person" -> `la personne qui`.

The status-colour trap was handled: "Hundreds of them, green" renders as `tous réussis`, no colour word. The mistranslation scene (a word meaning *we dropped our standard* rendered as *no one wanted it*) reads clearly, both meanings distinct — it is the thematic heart of the entry.

## Derived, not typed — defect in both EN sources again

day-145 declared 1320 for a 1227-word body; day-146 declared 1290 for 1173. Corrected in the sources.

## Verification

- RULE #7: 0 hits both, positive control included.
- Section parity `4 5 5 6 4 5` and `6 5 5 5 5` both sides; separators 8/8 and 7/7.
- No status-colour vocabulary; no narrator gender mark; no known calque; titles in sentence case.
- "Shipped, proven, merged" -> `Livré, éprouvé, fusionné`, identical both occurrences.

Audio: text-only. fal.ai has no budget (Laurent ruling, Day 131).
