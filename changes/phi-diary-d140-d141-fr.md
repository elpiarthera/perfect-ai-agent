# Diary Days 140 + 141 — publish EN + FR

- Day 140 "The Cure Carried the Illness" -> FR "Le remède portait la maladie"
- Day 141 "The Maker Left No Word" -> FR "L'artisan n'a laissé aucun mot"

## RULE #7 — clean, second time running

Independent sweep + full read of both entries: zero orchestrator names, zero client
names, positive control included. Days 139, 140 and 141 all arrived anonymised — the
upstream guard is holding.

## Defects caught

Two separate review passes, both reliable this time (every cited string existed).

Day 140:
- `Rien n'en a jamais refusé` — broken partitive pronoun, ambiguous. Now `Rien ne s'y est jamais opposé`.

Day 141:
- `le témoin s'est perdu dans la foule` for "the witness stood in the crowd" — **reversed meaning** (disappeared vs present-but-ignored). Now `se tenait dans la foule`.
- `Fini n'est pas visible` for "Done is not the same as fit to be seen" — dropped the parallel. Now `Achevé n'est pas la même chose que montrable`.
- `À l'un je dis` — present tense in a past-tense passage. Now `j'ai dit`.
- `qu'elle a passé` — pronoun disagreed with `le balayage` (masculine) and calqued "walked past". Now `qu'il a dépassé`.

## Structural repair (Day 140)

The translator split the last sentence of the "duplicate notes" section into its own
paragraph. Merged back: section parity restored to `6 4 2 3 3 3`, matching the source.

## Derived, not typed — and a defect in both EN sources

Both EN frontmatters carried inflated counts: day-140 declared 1337 for a 999-word body,
day-141 declared 1240 for 844. Corrected in the sources. FR counts derived from the file.

## Verification

- RULE #7: 0 hits both entries, positive control included.
- Section parity `6 4 2 3 3 3` and `4 3 3 3 3` on both sides; separators 8/8 and 7/7.
- No status-colour vocabulary; no narrator gender mark; no known calque.
- Final lines land: "A thing is not finished when it works. It is finished when it can be seen."

Audio: text-only. fal.ai has no budget (Laurent ruling, Day 131).
